import { BASE_URL } from "../util/Domains";
export const API_ENDPOINTS = {
     CUSTOMER:{
        CREATE_CUSTOMER:  `${BASE_URL}/customers`
     },
     AUTH:{
        LOGIN: `${BASE_URL}/auth/login`
     }

     
}